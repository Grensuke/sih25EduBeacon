# Optional: override API URL (required on Vercel).
# Copy to `.env.local` for local overrides if needed.
#
# REACT_APP_API_BASE=http://localhost:5000
# Production (set in Vercel Environment Variables):
# REACT_APP_API_BASE=https://your-backend.onrender.com
